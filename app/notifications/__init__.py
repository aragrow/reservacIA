"""Notification subsystem: durable queue + in-process worker + Spanish templates.

See docs/agent-app-plan.md (out of scope here — the notifications plan lives in
the project root /Users/david/.claude/plans/) for the full design.
"""
